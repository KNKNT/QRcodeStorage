<UserControl x:Class="QRcodeStorage.Views.ucCountOfCodes"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QRcodeStorage.Views"
             mc:Ignorable="d" 
             Height="40" MinWidth="500">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>

        <TextBlock x:Name="tblName" Text="Название товара" TextTrimming="CharacterEllipsis" FontSize="14" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="15,0,0,0"/>
        <TextBlock Grid.Column="1" FontSize="14" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="15,0,15,0">
            <Run x:Name="tblCount" Text="1"/>
            <Run Text=" шт."/>
        </TextBlock>

        <StackPanel Grid.Column="2" Margin="5">
            <Button x:Name="btnPlus"  Width="30" Height="15" Click="btnPlus_Click">
                <TextBlock Text="▴" VerticalAlignment="Center"/>
            </Button>
            <Button x:Name="btnMinus" Height="15" Click="btnMinus_Click">
                <TextBlock Text="▾" VerticalAlignment="Center"/>
            </Button>
        </StackPanel>
        <Border Height="1" Grid.ColumnSpan="3" Background="LightGray" VerticalAlignment="Bottom" Margin="5,0,5,0" />
    </Grid>
</UserControl>
